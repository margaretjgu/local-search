# Elasticsearch Configuration
ELASTICSEARCH_NODE=http://localhost:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=your_password_from_start_local_output
ELASTICSEARCH_API_KEY=your_api_key_from_start_local_output

# Server Configuration
PORT=3000
NODE_ENV=development

# NOTE: Use either username/password OR API key authentication
# If API key is provided, it will be used instead of username/password

# TROUBLESHOOTING:
# 1. Make sure Elasticsearch is running: curl http://localhost:9200
# 2. Check start-local output for correct credentials
# 3. Verify PORT is not in use: lsof -i :3000
